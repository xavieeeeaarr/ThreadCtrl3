<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Dashboard</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <link rel="stylesheet" href="~/css/site.css" />

    <style>
        body {
            margin: 0;
            background: #131211;
            color: #ffffff;
            font-family: Segoe UI, sans-serif;
            overflow-x: hidden;
        }

        .app-container {
            display: flex;
            height: 100vh;
            transition: all 0.3s ease;
        }

        /* Sidebar Styles */
        .sidebar {
            display: flex;
            flex-direction: column;
            width: 250px;
            background: #75911c;
            color: black !important;
            transition: all 0.3s ease;
            z-index: 1000;
            border-right: 1px solid rgba(0,0,0,0.1);
        }

        .sidebar-nav {
            flex: 1;
            overflow-y: auto;
        }

        .sidebar a {
            display: block;
            color: black !important;
            padding: 12px 20px;
            text-decoration: none;
            transition: background 0.2s;
        }

        .sidebar a:hover {
            background: rgba(0,0,0,0.1);
        }

        .logout-section {
            padding: 20px;
            border-top: 1px solid rgba(0,0,0,0.1);
        }

        /* Main Content Styles */
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            width: 100%;
            min-width: 0;
            background: #131211;
        }

        .content-area {
            padding: 20px;
            flex: 1;
            overflow-x: hidden;
            background: #131211 !important;
        }

        /* Top Bar Styles */
        .top-bar {
            background: #1a1a1a;
            border-bottom: 1px solid #333;
            padding: 10px 20px;
        }

        .top-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .system-name {
            color: #ffffff !important;
            font-weight: 600;
        }

        .search-input {
            background: #2a2a2a;
            border: 1px solid #444;
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            width: 100%;
            max-width: 300px;
        }

        .search-input::placeholder {
            color: #888;
        }

        /* Responsive Utilities */
        .table-responsive {
            display: block;
            width: 100%;
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
        }

        /* Toggle Button */
        .sidebar-toggle {
            background: transparent;
            border: none;
            font-size: 24px;
            color: #ffffff;
            padding: 10px;
            cursor: pointer;
        }

        /* Responsive Breakpoints */
        @@media (max-width: 991.98px) {
            .sidebar {
                position: fixed;
                left: -250px;
                top: 0;
                bottom: 0;
                box-shadow: 2px 0 10px rgba(0,0,0,0.5);
            }

            .sidebar.active {
                left: 0;
            }

            .sidebar-toggle {
                display: block;
            }

            .top-row {
                justify-content: space-between !important;
            }

            .dashboard-title {
                font-size: 16px !important;
            }

            /* Stack cards and grids on small mobile */
            @@media (max-width: 575.98px) {
                .cards-grid, .dashboard-grid {
                    grid-template-columns: 1fr !important;
                }
                .lower-grid {
                    grid-template-columns: 1fr !important;
                }
                .overview-header {
                    flex-direction: column;
                    align-items: flex-start !important;
                    gap: 15px;
                }
                .overview-actions {
                    width: 100%;
                }
                .overview-actions button {
                    width: 100%;
                    margin-bottom: 10px;
                }
            }
        }

        /* Overlay for mobile */
        .sidebar-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,0.7);
            z-index: 999;
        }

        .sidebar-overlay.active {
            display: block;
        }
    </style>

</head>

<body>

    <div class="sidebar-overlay" id="sidebarOverlay"></div>

    <div class="app-container">

        <!-- SIDEBAR -->
        <div class="sidebar text-black" id="sidebar">
            <div class="logo-main ms-4 mt-2 mb-2">
                <div class="d-flex justify-content-between align-items-center">
                    <img src="~/images/logo.png" alt="Logo" style="max-width: 150px;" />
                    <button class="btn d-lg-none text-black" id="closeSidebar">
                        <i class="bi bi-x-lg"></i>
                    </button>
                </div>
            </div>

            <div class="sidebar-nav">
                <a asp-controller="Test" asp-action="dashboard" class="text-black"><i class="bi bi-clipboard-data-fill"></i> Dashboard</a>
                
                @if (User.IsInRole("Super Admin") || User.IsInRole("Company Admin") || User.IsInRole("Inventory Manager") || User.IsInRole("Auditor"))
                {
                    <a asp-controller="Test" asp-action="Report" class="text-black"><i class="bi bi-file-earmark-bar-graph-fill"></i> Reports & Valuation</a>
                }

                @if (User.IsInRole("Super Admin") || User.IsInRole("Company Admin") || User.IsInRole("Inventory Manager") || User.IsInRole("Sales Staff") || User.IsInRole("Procurement Officer") || User.IsInRole("Auditor"))
                {
                    <a asp-controller="Test" asp-action="Inventory" class="text-black"><i class="bi bi-archive-fill"></i> Inventory</a>
                }

                @if (User.IsInRole("Super Admin") || User.IsInRole("Company Admin") || User.IsInRole("Sales Staff"))
                {
                    <a asp-controller="Test" asp-action="Sales" class="text-black"><i class="bi bi-cart-dash-fill"></i> Sales Orders</a>
                }

                @if (User.IsInRole("Super Admin") || User.IsInRole("Company Admin") || User.IsInRole("Procurement Officer"))
                {
                    <a asp-controller="Test" asp-action="Procurement" class="text-black"><i class="bi bi-basket2-fill"></i> Procurement</a>
                }

                @if (User.IsInRole("Super Admin") || User.IsInRole("Company Admin") || User.IsInRole("Inventory Manager") || User.IsInRole("Auditor"))
                {
                    <a asp-controller="Test" asp-action="Stock" class="text-black"><i class="bi bi-box-seam-fill"></i> Stock Adjustments</a>
                }

                @if (User.IsInRole("Super Admin") || User.IsInRole("Company Admin"))
                {
                    <a asp-controller="Test" asp-action="UserManagement" class="text-black"><i class="bi bi-person-fill-add"></i> User Management</a>
                }

                @if (User.IsInRole("Super Admin") || User.IsInRole("Company Admin") || User.IsInRole("Auditor"))
                {
                    <a asp-controller="Test" asp-action="Security" class="text-black"><i class="bi bi-shield-fill-exclamation"></i> Security & Audit</a>
                }

                @if (User.IsInRole("Super Admin") || User.IsInRole("Company Admin") || User.IsInRole("Inventory Manager"))
                {
                    <a asp-controller="Test" asp-action="Settings" class="text-black"><i class="bi bi-gear-fill"></i> Settings</a>
                }
            </div>

            <div class="logout-section">
                <a asp-controller="Test" asp-action="Logout" class="btn btn-danger w-100 text-white">
                    <i class="bi bi-box-arrow-right"></i> Logout
                </a>
            </div>
        </div>

        <!-- MAIN -->
        <div class="main-content">

            <div class="top-bar">
                <div class="top-row">
                    <div class="d-flex align-items-center">
                        <button class="sidebar-toggle me-2" id="toggleSidebar">
                            <i class="bi bi-list"></i>
                        </button>
                        <div class="dashboard-title system-name text-white">
                            @ViewData["Title"]
                        </div>
                    </div>

                    <div class="text-white d-flex align-items-center gap-2">
                        <i class="bi bi-person-circle fs-4"></i>
                        <div class="d-flex flex-column align-items-start lh-1">
                            <div class="fw-bold fw-normal" style="font-size: 0.9rem;">Hello, @(User.Identity?.Name ?? "User")</div>
                            @{
                                var company = User.FindFirst("CompanyName")?.Value;
                                var role = User.Claims.FirstOrDefault(c => c.Type == System.Security.Claims.ClaimTypes.Role)?.Value;
                            }
                            <div class="text-white-50" style="font-size: 0.75rem;">
                                @(string.IsNullOrEmpty(company) ? "" : company + " | ") @role
                            </div>
                        </div>
                    </div>
                </div>

                <div class="bottom-row">
                    <input type="text" class="search-input" placeholder="Search...">
                </div>
            </div>

            <div class="content-area">
                @RenderBody()
            </div>

        </div>

    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        $(document).ready(function() {
            function toggleSidebar() {
                $('#sidebar').toggleClass('active');
                $('#sidebarOverlay').toggleClass('active');
            }

            $('#toggleSidebar, #closeSidebar, #sidebarOverlay').on('click', function() {
                toggleSidebar();
            });

            // Close sidebar when clicking a nav link on mobile
            $('.sidebar-nav a').on('click', function() {
                if ($(window).width() < 992) {
                    toggleSidebar();
                }
            });
        });
    </script>
</body>
</html>
